<div class="ui grid stackable padded">
    <div class="ui breadcrumb">
    <a href="/dashboard" class="section">Home</a>
    <div class="divider"> / </div>
    <a href="{{breadcrumb}}" class="section">{{breadcrumb}}</a>
    </div>
</div>   